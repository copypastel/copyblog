#media.section
  .header
    %b //
    the
    %b{ :style => 'color: #ff7b00'}> media
  .flickr_thumbnail
    - @flickr_images.each do |img|
      %a{ :href => img.url}
        %img{ :src => img.img_src, :alt => img.alt}
